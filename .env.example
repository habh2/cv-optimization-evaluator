# Google Gemini
GOOGLE_API_KEY=your-google-api-key-here

# Models
LLM_MODEL=gemini-2.5-flash
EMBEDDING_MODEL=gemini-embedding-2

# Scorer prompt version — bump whenever SCORER_PROMPT changes to invalidate cached scores
SCORER_VERSION=v1.1
